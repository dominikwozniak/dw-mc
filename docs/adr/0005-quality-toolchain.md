# One `pnpm check` chain gates the repo, and oxlint carries the Effect diagnostics

`pnpm check` runs lint, format, typecheck, test and build. CI runs that one script, so a link added to the chain in `package.json` runs in CI without touching a workflow, and a ruleset on `main` requires the `Check` job. The pre-commit hook runs `lint-staged` alone: a hook that typechecks and tests is a hook people pass `--no-verify` to.

Linting is oxlint, because Effect's own monorepo lints with it and `@effect/tsgo` publishes the Effect rules as oxlint presets. `prepare` runs `effect-tsgo patch --oxlint`, which rewrites the installed oxlint binary to call the Effect TypeScript-Go engine and injects the `effecttsgo` namespace into oxlint's plugin enum; the config is invalid without it. The patcher validates a version matrix and refuses on a mismatch, so oxlint and oxlint-tsgolint are pinned exactly.

The gate also holds what the other decision records decide, because an invariant that lives only in a Markdown file is one an agent reads once. Two ADR 0006 invariants are `no-restricted-imports` configuration and need no code: a module in `src/` is named rather than counted, and the platform services belong to the entry point. ADR 0002's write boundary needs a rule, because oxlint has no `no-restricted-syntax`: `dw-mc/no-gh-writes` reads the argument vector handed to `gh` against an allow list held in the rule file and refuses everything else, so a verb nobody has thought about waits to be read rather than arriving by silence. It reads the vector where it is written, and one spelling is out of reach — a `gh` bound to a variable first, which would cost the scope analysis the rule is thin for avoiding. Four further rules are copied from `anti-slop` into `tools/oxlint/anti-slop/`, by hand rather than installed because there is no package: a type assertion states the invariant it stands on, assertions do not chain, a test replaces a dependency at a real seam rather than mocking the module, and a ladder of literal ternaries over one value is a `Match`. Every rule lands at `error`, and all but one report nothing on arrival, so there is no count to carry and no reason for a warning rung. Every diagnostic names the record it protects, because the record is what to go read.

Effect diagnostics are reported by oxlint alone. The same findings are available from `tsc` through the language-service plugin and from `effect-tsgo diagnostics`, but the three are peers and only oxlint can fix them. `tsconfig.json` sets `"diagnostics": false` on the plugin so the editor does not report each finding twice; hover and the rest of the language service stay on.

Formatting is oxfmt, which comes from the same project as oxlint and is pinned in the lockfile the same way. One binary covers every file this repo keeps — TypeScript, JSON, Markdown and YAML — so there is no plugin list to maintain and nothing is fetched over the network to format a file. It sorts imports and `package.json` keys as part of formatting, which no dprint plugin does.

The price is the Effect style. oxfmt prints Prettier's shape, so `src/` does not hug combinators the way `repos/effect` does, and no option brings that back. Keeping dprint and moving it onto dprint's own oxc plugin is the alternative that would have preserved the tooling, and it recovers none of the style: with matching options the two emit byte-identical output. It is worse on everything else — the plugin claims only JavaScript and TypeScript, so Markdown, JSON and YAML stay on separate plugins and the list grows rather than goes away; its version number does not name the oxc it carries; and it cannot sort `package.json`.

`minimumReleaseAge` is three days rather than the week that would be stronger. `effect` ships a release candidate every few days and vitest 5 pulls the rolldown toolchain, so a week rejects 33 lockfile entries on an ordinary day and `minimumReleaseAgeExclude` stops being a list of decisions.

## Consequences

- An override replaces a rule's configuration in full rather than merging into it, and nothing says so when it bites. `no-restricted-imports` is already claimed by an override in `src/adapters/` and `src/domain/`, so a root-level pattern is dead inside exactly those two directories; every block that names the rule repeats the whole pattern set, and the blocks match disjoint sets of files so that no file's configuration turns on which override oxlint applies last. A version of the source-layout guards that set only the root reported nothing where it mattered and looked like it worked.
- The vendored rules need one exclusion each in [`.oxlintrc.json`](../../.oxlintrc.json), [`.oxfmtrc.json`](../../.oxfmtrc.json) and [`tsconfig.check.json`](../../tsconfig.check.json); the `repos` entry the first two already carry does not reach them. They are not excluded from the suite: they arrive with upstream's own tests, which is what makes a refresh verifiable rather than trusted, and `tools/oxlint/rule-tester.setup.ts` hands oxlint's `RuleTester` the `describe` and `it` that vitest keeps off `globalThis`. `tools/oxlint/anti-slop/UPSTREAM.md` records the revision, what a refresh may take, and which files are this repository's.
- `tsconfig.check.json` is the only place `tools/` is typechecked. `tsconfig.json` is the build, and a lint plugin is not part of what `dist` ships, so a plugin file that does not compile fails `pnpm typecheck` and never fails `pnpm build`.
- `@oxlint/plugins` is pinned to the exact installed oxlint version, the way `oxlint-tsgolint` already is. The two move together and the patcher's version matrix keeps refusing a mismatch.
- The two configuration guards have no automated test, because a fixture that must fail lint cannot live in `src/` without failing the gate. Both were verified firing against a scratch file: an import spelled `../adapters/spawner.ts` and an import of `@effect/platform-node` outside `src/cli/bin.ts` each fail `pnpm lint`, and each still fails inside `src/adapters/` and `src/domain/`, which is where the override trap hides. The five rules were verified the same way, in one scratch file that trips all of them.
- Two keys in `.oxlintrc.json` are load-bearing and silent when wrong. `options.typeAware` is root-config-only, so an extended preset cannot supply it, and without it ~97 rules load, enable and do nothing. `plugins` at the root replaces the inherited set instead of merging, so `effecttsgo` is named there as well. A green run proves neither; the check is a floating `Effect` in a scratch file firing `effecttsgo/floating-effect`.
- The lint and format scripts both pass `--disable-nested-config`, because oxlint and oxfmt both read configuration out of subdirectories before they apply ignore patterns. `repos/effect` already carries an `.oxlintrc.json` wiring a workspace-internal plugin that cannot resolve here; it carries no `.oxfmtrc.json` today, and the day it does, a bare `oxfmt` run formats 4026 files instead of 42.
- The `imports` map in `package.json` needs its `development` condition, and nothing fails when it is missing. `#*.ts` resolves to `./dist/*.js` by default so the built CLI runs under plain node, and vitest picks `development` to reach `./src/*.ts` instead. Drop that condition and the suite still passes — against whatever `dist` was built last. The check is to delete `dist` and run `pnpm test`: it must fail to resolve, not pass.
- `categories` reaches past the preset: a rule in `correctness` or `suspicious` turns on even when `recommended.json` omits it. The Effect idioms that arrive that way are switched off by name, each with its reason.
- `embeddedLanguageFormatting` is `"off"`, and only Markdown depends on it. On the default `"auto"` oxfmt formats fenced code blocks, which reflows the annotated YAML sample in `docs/v1-design.md` and strips the comment alignment that carries its meaning. Off, every Markdown file here formats to no change at all.
- Both tools fail when every file they are handed turns out to be excluded — oxfmt exits 2, oxlint exits 1 — so `lint-staged` passes `--no-error-on-unmatched-pattern` to each. oxfmt excludes lockfiles by a rule of its own, and a commit staging only `pnpm-lock.yaml` hits that half; a commit that refreshes nothing but the vendored rules hits the linter's. The flag suppresses the empty-selection error alone: a staged file that breaks a rule still fails the hook.
- oxfmt reads `.gitignore`, so `ignorePatterns` carries only what is tracked and still not ours: `repos`, `.claude` and the vendored rules. Without the first two it formats 1792 vendored files.
- `trustPolicy: no-downgrade` costs one standing exemption: `undici-types@6.21.0`, pinned by `@types/node@22.20.2`, shipped before that package published with provenance.
