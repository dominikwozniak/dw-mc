---
"dw-mc": patch
---

`dw-mc forget <pr>` answers a configuration file it cannot read the way every other command does: one sentence naming the file and the key that is wrong, instead of an error with a stack trace.
