---
"dw-mc": minor
---

`dw-mc status` and the picker mark what moved since you last looked. A row whose bucket or whose facts changed carries `*`, a pull request never shown carries `+`, and a line above the group says where the row came from and what moved. `dw-mc sweep` shows nothing, so it leaves the mark where it was.
